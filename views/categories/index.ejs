<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body class="container">
    <header>
      <% include ../partials/header %>
    </header>
    <main>
      <div class="jumbotron">
        <h3>Categories</h3>
        <p>List of categories</p>
      </div>
      <% docs.map((category) => { %>
        <p>
          Name: <%= category.category_name %> (ID: <%= category._id %>)<br />
          Url: <%= category.url %><br />
          <a href="/products?category_id=<%= category._id %>">
            View
          </a>
        </p>
        <hr />
      <% }) %>
    </main>

    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
